export default function RootLoading() {
  return (
    <main className="flex min-h-screen items-center justify-center px-6">
      <div className="rounded-[1.75rem] border border-white/60 bg-white/80 px-6 py-5 shadow-panel">
        <p className="text-sm uppercase tracking-[0.24em] text-accent">MoveScript</p>
        <h1 className="mt-2 text-2xl font-semibold text-ink">페이지를 불러오는 중...</h1>
      </div>
    </main>
  );
}
